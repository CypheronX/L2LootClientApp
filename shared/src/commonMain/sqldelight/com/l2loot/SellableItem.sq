
CREATE TABLE sellable_item (
    item_id INTEGER PRIMARY KEY NOT NULL,
    name TEXT NOT NULL,
    item_price INTEGER NOT NULL
);

CREATE INDEX sellable_item_id_idx ON sellable_item(item_id);

insert:
INSERT INTO sellable_item (item_id, name, item_price)
VALUES (?, ?, ?);